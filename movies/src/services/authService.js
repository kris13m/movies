/*import { axiosSingleton } from './apiClient';

export const login = (credentials) => {
  return axiosSingleton.post('/auth/login', credentials);
};

export const register = (credentials) => {
  return axiosSingleton.post('/auth/register', credentials);
};

export const logout = () => {
  return axiosSingleton.post('/auth/logout', {});
};

export const getSession = () => {
  return axiosSingleton.get('/auth/session');
};*/